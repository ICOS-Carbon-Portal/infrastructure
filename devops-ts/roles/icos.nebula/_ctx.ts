// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.nebula/{defaults,vars}/*.yml
// Per-role variable context: 21 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  nebula_bin_dir: string;
  nebula_ca_path: Tmpl;
  nebula_cert_min_days: number;
  nebula_domain: string;
  nebula_etc_dir: string;
  nebula_fw_enable: boolean;
  nebula_hostname: Tmpl;
  nebula_hosts_enable: boolean;
  nebula_is_lighthouse: boolean;
  nebula_resolve_enable: boolean;
  nebula_resolve_servers: null;
  nebula_resolve_test: null;
  nebula_resolve_type: string;
  nebula_ssh_key: Tmpl;
  nebula_ssh_port: number;
  nebula_stats_enable: boolean;
  nebula_stats_port: number;
  nebula_upgrade: Tmpl;
  nebula_url_map: Record<string, Tmpl>;
  nebula_user: string;
  nebula_version: Tmpl;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const nebula_bin_dir = vref("nebula_bin_dir");
export const nebula_ca_path = vref("nebula_ca_path");
export const nebula_cert_min_days = vref("nebula_cert_min_days");
export const nebula_domain = vref("nebula_domain");
export const nebula_etc_dir = vref("nebula_etc_dir");
export const nebula_fw_enable = vref("nebula_fw_enable");
export const nebula_hostname = vref("nebula_hostname");
export const nebula_hosts_enable = vref("nebula_hosts_enable");
export const nebula_is_lighthouse = vref("nebula_is_lighthouse");
export const nebula_resolve_enable = vref("nebula_resolve_enable");
export const nebula_resolve_servers = vref("nebula_resolve_servers");
export const nebula_resolve_test = vref("nebula_resolve_test");
export const nebula_resolve_type = vref("nebula_resolve_type");
export const nebula_ssh_key = vref("nebula_ssh_key");
export const nebula_ssh_port = vref("nebula_ssh_port");
export const nebula_stats_enable = vref("nebula_stats_enable");
export const nebula_stats_port = vref("nebula_stats_port");
export const nebula_upgrade = vref("nebula_upgrade");
export const nebula_url_map = vref("nebula_url_map");
export const nebula_user = vref("nebula_user");
export const nebula_version = vref("nebula_version");

// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.drupal/{defaults,vars}/*.yml
// Per-role variable context: 17 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  certbot_conf_name: Tmpl;
  certbot_domains: Tmpl;
  database_name: string;
  database_user: string;
  domain: string;
  drupal_docker_port: number;
  drupal_home: string;
  drupal_modules: unknown;
  drupal_version: number;
  drupal_websites: unknown;
  mysql_docker_port: number;
  nginx_conf_name: string;
  project_dir: Tmpl;
  robots_txt: string;
  ssl_domains: unknown;
  trusted_host: string;
  update: boolean;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const certbot_conf_name = vref("certbot_conf_name");
export const certbot_domains = vref("certbot_domains");
export const database_name = vref("database_name");
export const database_user = vref("database_user");
export const domain = vref("domain");
export const drupal_docker_port = vref("drupal_docker_port");
export const drupal_home = vref("drupal_home");
export const drupal_modules = vref("drupal_modules");
export const drupal_version = vref("drupal_version");
export const drupal_websites = vref("drupal_websites");
export const mysql_docker_port = vref("mysql_docker_port");
export const nginx_conf_name = vref("nginx_conf_name");
export const project_dir = vref("project_dir");
export const robots_txt = vref("robots_txt");
export const ssl_domains = vref("ssl_domains");
export const trusted_host = vref("trusted_host");
export const update = vref("update");

// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.quince/{defaults,vars}/*.yml
// Per-role variable context: 11 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  bbclient_name: string;
  bbclient_user: Tmpl;
  quince_backup_enable: boolean;
  quince_filestore: string;
  quince_home: string;
  quince_jdk_version: number;
  quince_log_file: string;
  quince_slack_url: string;
  quince_tomcat_home: string;
  quince_tomcat_url: string;
  quince_user: string;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const bbclient_name = vref("bbclient_name");
export const bbclient_user = vref("bbclient_user");
export const quince_backup_enable = vref("quince_backup_enable");
export const quince_filestore = vref("quince_filestore");
export const quince_home = vref("quince_home");
export const quince_jdk_version = vref("quince_jdk_version");
export const quince_log_file = vref("quince_log_file");
export const quince_slack_url = vref("quince_slack_url");
export const quince_tomcat_home = vref("quince_tomcat_home");
export const quince_tomcat_url = vref("quince_tomcat_url");
export const quince_user = vref("quince_user");

// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.onlyoffice/{defaults,vars}/*.yml
// Per-role variable context: 5 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  onlyoffice_build_dir: Tmpl;
  onlyoffice_domain: unknown;
  onlyoffice_home: string;
  onlyoffice_port: number;
  onlyoffice_version: string;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const onlyoffice_build_dir = vref("onlyoffice_build_dir");
export const onlyoffice_domain = vref("onlyoffice_domain");
export const onlyoffice_home = vref("onlyoffice_home");
export const onlyoffice_port = vref("onlyoffice_port");
export const onlyoffice_version = vref("onlyoffice_version");

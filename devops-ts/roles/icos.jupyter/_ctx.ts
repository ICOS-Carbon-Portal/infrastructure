// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.jupyter/{defaults,vars}/*.yml
// Per-role variable context: 7 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";
import type { VarValue } from "../../lib/ansible/values.ts";

export interface Vars {
  jupyter_home: string;
  jupyter_hub_config: Record<string, VarValue>;
  jupyter_hub_config_defaults: Record<string, VarValue>;
  jupyter_jusers_enable: boolean;
  jupyter_port: unknown;
  jusers_home: string;
  jusers_venv: Tmpl;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const jupyter_home = vref("jupyter_home");
export const jupyter_hub_config = vref("jupyter_hub_config");
export const jupyter_hub_config_defaults = vref("jupyter_hub_config_defaults");
export const jupyter_jusers_enable = vref("jupyter_jusers_enable");
export const jupyter_port = vref("jupyter_port");
export const jusers_home = vref("jusers_home");
export const jusers_venv = vref("jusers_venv");

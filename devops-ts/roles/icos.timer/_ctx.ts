// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.timer/{defaults,vars}/*.yml
// Per-role variable context: 8 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  _timer_sysd_service: Tmpl;
  _timer_sysd_timer: Tmpl;
  timer_desc: Tmpl;
  timer_dest: Tmpl;
  timer_exec: Tmpl;
  timer_home: string;
  timer_user: string;
  timer_wdir: null;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const _timer_sysd_service = vref("_timer_sysd_service");
export const _timer_sysd_timer = vref("_timer_sysd_timer");
export const timer_desc = vref("timer_desc");
export const timer_dest = vref("timer_dest");
export const timer_exec = vref("timer_exec");
export const timer_home = vref("timer_home");
export const timer_user = vref("timer_user");
export const timer_wdir = vref("timer_wdir");

// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.restic_server/{defaults,vars}/*.yml
// Per-role variable context: 11 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  restic_server_architecture: Tmpl;
  restic_server_data: null;
  restic_server_exec: Tmpl;
  restic_server_home: string;
  restic_server_htpasswd: Tmpl;
  restic_server_listen: number;
  restic_server_upgrade: Tmpl;
  restic_server_url_map: Record<string, Tmpl>;
  restic_server_user: string;
  restic_server_users: unknown;
  restic_server_version: Tmpl;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const restic_server_architecture = vref("restic_server_architecture");
export const restic_server_data = vref("restic_server_data");
export const restic_server_exec = vref("restic_server_exec");
export const restic_server_home = vref("restic_server_home");
export const restic_server_htpasswd = vref("restic_server_htpasswd");
export const restic_server_listen = vref("restic_server_listen");
export const restic_server_upgrade = vref("restic_server_upgrade");
export const restic_server_url_map = vref("restic_server_url_map");
export const restic_server_user = vref("restic_server_user");
export const restic_server_users = vref("restic_server_users");
export const restic_server_version = vref("restic_server_version");

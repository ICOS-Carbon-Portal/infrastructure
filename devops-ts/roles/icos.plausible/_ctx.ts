// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.plausible/{defaults,vars}/*.yml
// Per-role variable context: 10 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  plausible_backup_enabled: boolean;
  plausible_domain: string;
  plausible_google_client_id: Tmpl;
  plausible_google_client_secret: Tmpl;
  plausible_home: string;
  plausible_mailer_email: Tmpl;
  plausible_port: number;
  plausible_postgres_password: Tmpl;
  plausible_secret: Tmpl;
  plausible_totp_key: Tmpl;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const plausible_backup_enabled = vref("plausible_backup_enabled");
export const plausible_domain = vref("plausible_domain");
export const plausible_google_client_id = vref("plausible_google_client_id");
export const plausible_google_client_secret = vref(
  "plausible_google_client_secret",
);
export const plausible_home = vref("plausible_home");
export const plausible_mailer_email = vref("plausible_mailer_email");
export const plausible_port = vref("plausible_port");
export const plausible_postgres_password = vref("plausible_postgres_password");
export const plausible_secret = vref("plausible_secret");
export const plausible_totp_key = vref("plausible_totp_key");

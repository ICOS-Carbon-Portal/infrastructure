// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.wireguard/{defaults,vars}/*.yml
// Per-role variable context: 3 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  _wg_is_installed: boolean;
  wireguard_reresolve_script: string;
  wireguard_update: boolean;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const _wg_is_installed = vref("_wg_is_installed");
export const wireguard_reresolve_script = vref("wireguard_reresolve_script");
export const wireguard_update = vref("wireguard_update");

// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.jarservice/{defaults,vars}/*.yml
// Per-role variable context: 4 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  certbot_disabled: boolean;
  extra_groups: string;
  jarservice_jar: Tmpl;
  jarservice_keep_n_old: number;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const certbot_disabled = vref("certbot_disabled");
export const extra_groups = vref("extra_groups");
export const jarservice_jar = vref("jarservice_jar");
export const jarservice_keep_n_old = vref("jarservice_keep_n_old");

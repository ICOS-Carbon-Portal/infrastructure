// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.mailman/{defaults,vars}/*.yml
// Per-role variable context: 26 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  mailman_admin_email: Tmpl;
  mailman_build_core: Tmpl;
  mailman_build_web: Tmpl;
  mailman_certbot_name: string;
  mailman_core_ipv4: string;
  mailman_database_ipv4: string;
  mailman_django_settings: Tmpl;
  mailman_home: string;
  mailman_host_ipv4: string;
  mailman_hyperkitty_api_key: Tmpl;
  mailman_nginxsite_file: string;
  mailman_nginxsite_name: string;
  mailman_postfix_config: unknown;
  mailman_postgres_password: Tmpl;
  mailman_rest_allow_deny: null;
  mailman_rest_port: number;
  mailman_rest_user: string;
  mailman_serve_from_domain: Tmpl;
  mailman_subnet: string;
  mailman_user: string;
  mailman_version: string;
  mailman_volume_core: Tmpl;
  mailman_volume_database: Tmpl;
  mailman_volume_web: Tmpl;
  mailman_web_ipv4: string;
  mailman_web_secret_key: Tmpl;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const mailman_admin_email = vref("mailman_admin_email");
export const mailman_build_core = vref("mailman_build_core");
export const mailman_build_web = vref("mailman_build_web");
export const mailman_certbot_name = vref("mailman_certbot_name");
export const mailman_core_ipv4 = vref("mailman_core_ipv4");
export const mailman_database_ipv4 = vref("mailman_database_ipv4");
export const mailman_django_settings = vref("mailman_django_settings");
export const mailman_home = vref("mailman_home");
export const mailman_host_ipv4 = vref("mailman_host_ipv4");
export const mailman_hyperkitty_api_key = vref("mailman_hyperkitty_api_key");
export const mailman_nginxsite_file = vref("mailman_nginxsite_file");
export const mailman_nginxsite_name = vref("mailman_nginxsite_name");
export const mailman_postfix_config = vref("mailman_postfix_config");
export const mailman_postgres_password = vref("mailman_postgres_password");
export const mailman_rest_allow_deny = vref("mailman_rest_allow_deny");
export const mailman_rest_port = vref("mailman_rest_port");
export const mailman_rest_user = vref("mailman_rest_user");
export const mailman_serve_from_domain = vref("mailman_serve_from_domain");
export const mailman_subnet = vref("mailman_subnet");
export const mailman_user = vref("mailman_user");
export const mailman_version = vref("mailman_version");
export const mailman_volume_core = vref("mailman_volume_core");
export const mailman_volume_database = vref("mailman_volume_database");
export const mailman_volume_web = vref("mailman_volume_web");
export const mailman_web_ipv4 = vref("mailman_web_ipv4");
export const mailman_web_secret_key = vref("mailman_web_secret_key");

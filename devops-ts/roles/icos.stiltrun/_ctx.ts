// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.stiltrun/{defaults,vars}/*.yml
// Per-role variable context: 5 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  stiltrun_image_id: string;
  stiltrun_image_name: string;
  stiltrun_image_url: Tmpl;
  stiltrun_input_dir: Tmpl;
  stiltrun_user: string;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const stiltrun_image_id = vref("stiltrun_image_id");
export const stiltrun_image_name = vref("stiltrun_image_name");
export const stiltrun_image_url = vref("stiltrun_image_url");
export const stiltrun_input_dir = vref("stiltrun_input_dir");
export const stiltrun_user = vref("stiltrun_user");

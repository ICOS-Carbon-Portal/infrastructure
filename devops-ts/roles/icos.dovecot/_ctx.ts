// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.dovecot/{defaults,vars}/*.yml
// Per-role variable context: 9 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  dovecot_auth_file: string;
  dovecot_cert_file: string;
  dovecot_domains: unknown;
  dovecot_domains_file: string;
  dovecot_lmtp: string;
  dovecot_log_file: string;
  dovecot_port: number;
  dovecot_vmail_home: string;
  dovecot_vmail_name: string;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const dovecot_auth_file = vref("dovecot_auth_file");
export const dovecot_cert_file = vref("dovecot_cert_file");
export const dovecot_domains = vref("dovecot_domains");
export const dovecot_domains_file = vref("dovecot_domains_file");
export const dovecot_lmtp = vref("dovecot_lmtp");
export const dovecot_log_file = vref("dovecot_log_file");
export const dovecot_port = vref("dovecot_port");
export const dovecot_vmail_home = vref("dovecot_vmail_home");
export const dovecot_vmail_name = vref("dovecot_vmail_name");

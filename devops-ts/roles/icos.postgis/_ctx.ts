// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.postgis/{defaults,vars}/*.yml
// Per-role variable context: 8 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  postgis_db_name: string;
  postgis_db_pass: Tmpl;
  postgis_db_user: string;
  postgis_db_users: unknown;
  postgis_home: string;
  postgis_package: string;
  postgis_postgres_version: number;
  postgresql_postgis_enable: boolean;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const postgis_db_name = vref("postgis_db_name");
export const postgis_db_pass = vref("postgis_db_pass");
export const postgis_db_user = vref("postgis_db_user");
export const postgis_db_users = vref("postgis_db_users");
export const postgis_home = vref("postgis_home");
export const postgis_package = vref("postgis_package");
export const postgis_postgres_version = vref("postgis_postgres_version");
export const postgresql_postgis_enable = vref("postgresql_postgis_enable");

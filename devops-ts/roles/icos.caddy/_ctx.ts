// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.caddy/{defaults,vars}/*.yml
// Per-role variable context: 8 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  caddy_bin: Tmpl;
  caddy_dropin_path: string;
  caddy_global_conf: string;
  caddy_modules: unknown;
  caddy_site_state: string;
  caddy_upgrade: Tmpl;
  caddy_via_xcaddy: string;
  xcaddy_upgrade: Tmpl;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const caddy_bin = vref("caddy_bin");
export const caddy_dropin_path = vref("caddy_dropin_path");
export const caddy_global_conf = vref("caddy_global_conf");
export const caddy_modules = vref("caddy_modules");
export const caddy_site_state = vref("caddy_site_state");
export const caddy_upgrade = vref("caddy_upgrade");
export const caddy_via_xcaddy = vref("caddy_via_xcaddy");
export const xcaddy_upgrade = vref("xcaddy_upgrade");

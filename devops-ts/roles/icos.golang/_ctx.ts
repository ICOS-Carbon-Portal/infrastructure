// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.golang/{defaults,vars}/*.yml
// Per-role variable context: 7 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  golang_apt_version_ok: Tmpl;
  golang_bin_dir: Tmpl;
  golang_local_version_ok: Tmpl;
  golang_min_version: Tmpl;
  golang_opt_dir: Tmpl;
  golang_url: Tmpl;
  golang_version_install: string;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const golang_apt_version_ok = vref("golang_apt_version_ok");
export const golang_bin_dir = vref("golang_bin_dir");
export const golang_local_version_ok = vref("golang_local_version_ok");
export const golang_min_version = vref("golang_min_version");
export const golang_opt_dir = vref("golang_opt_dir");
export const golang_url = vref("golang_url");
export const golang_version_install = vref("golang_version_install");

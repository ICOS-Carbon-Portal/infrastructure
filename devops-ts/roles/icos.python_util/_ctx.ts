// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.python_util/{defaults,vars}/*.yml
// Per-role variable context: 4 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  python_util_bin_dir: string;
  python_util_install_dir: Tmpl;
  python_util_install_prefix: string;
  python_util_python_executable: string;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const python_util_bin_dir = vref("python_util_bin_dir");
export const python_util_install_dir = vref("python_util_install_dir");
export const python_util_install_prefix = vref("python_util_install_prefix");
export const python_util_python_executable = vref(
  "python_util_python_executable",
);

// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.flexpart/{defaults,vars}/*.yml
// Per-role variable context: 6 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  flexpart_container_prefix: string;
  flexpart_export_output_to: null;
  flexpart_image: string;
  flexpart_max_parallel: number;
  flexpart_output_directory: string;
  flexpart_user: string;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const flexpart_container_prefix = vref("flexpart_container_prefix");
export const flexpart_export_output_to = vref("flexpart_export_output_to");
export const flexpart_image = vref("flexpart_image");
export const flexpart_max_parallel = vref("flexpart_max_parallel");
export const flexpart_output_directory = vref("flexpart_output_directory");
export const flexpart_user = vref("flexpart_user");

// Auto-generated by gen-contexts.ts from
// ../../../devops/roles/icos.script_exporter/{defaults,vars}/*.yml
// Per-role variable context: 8 own variables, each exported as
// a named binding. Reference Ansible variables by importing them (no V
// proxy); the Vars interface is the type this role's data satisfies.
import { type Tmpl, varProxy, type VarRef } from "../../lib/template.ts";

export interface Vars {
  sexp_arch: Tmpl;
  sexp_arch_map: Record<string, string>;
  sexp_config_file: Tmpl;
  sexp_home: string;
  sexp_listen: string;
  sexp_scripts_enable: unknown;
  sexp_scripts_repo: Tmpl;
  sexp_scripts_venv: Tmpl;
}

const vref = <K extends keyof Vars>(k: K): VarRef<Vars[K]> =>
  varProxy(k) as VarRef<Vars[K]>;

export const sexp_arch = vref("sexp_arch");
export const sexp_arch_map = vref("sexp_arch_map");
export const sexp_config_file = vref("sexp_config_file");
export const sexp_home = vref("sexp_home");
export const sexp_listen = vref("sexp_listen");
export const sexp_scripts_enable = vref("sexp_scripts_enable");
export const sexp_scripts_repo = vref("sexp_scripts_repo");
export const sexp_scripts_venv = vref("sexp_scripts_venv");

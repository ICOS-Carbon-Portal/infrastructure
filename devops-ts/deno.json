{
  "tasks": {
    "check": "deno check lib/*.ts playbooks/*.ts render.ts verify.ts",
    "verify": "deno run --allow-read --allow-env verify.ts",
    "render": "deno run --quiet --allow-all render.ts"
  },
  "fmt": {
    "include": ["lib/", "playbooks/", "render.ts"]
  }
}

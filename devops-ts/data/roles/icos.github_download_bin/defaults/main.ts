// Auto-generated by gen-data.ts from ../devops/roles/icos.github_download_bin/defaults/main.yml
import type { VarsFile } from "../../../../lib/data.ts";
import { tmpl } from "../../../../lib/ansible.ts";

export default {
  "dbin_download_base": "/opt/downloads",
  "dbin_download_dest": tmpl("{{ dbin_download_base }}/{{ _dbin_name }}"),
  "dbin_bin_dir": "/usr/local/bin",
  "dbin_default_url": tmpl(
    "{{ dbin__down }}/v{{ dbin__vers }}/{{ dbin_repo }}-{{ dbin__vers }}.{{ dbin__plat }}.tar.gz",
  ),
  "dbin_arch": "amd64",
  "_dbin_url": tmpl("{{ dbin_url | default(dbin_default_url) }}"),
  "_dbin_unar": tmpl("{{ dbin_unar | default(True) }}"),
  "_dbin_path": tmpl("{{ dbin_path | default(dbin_repo) }}"),
  "_dbin_name": tmpl("{{ _dbin_path | basename }}"),
  "_dbin_src": tmpl(
    "{% if dbin_src is defined -%}\n{{ dbin_download_dest }}/{{ dbin_src -}}\n{% elif _dbin_unar -%}\n{{ dbin_download_dest }}/{{ _unar.files[0].rstrip('/') }}/{{ _dbin_name -}}\n{%- else %}\n{{- dbin_download.dest }}\n{%- endif %}",
  ),
  "dbin__down": tmpl(
    "https://github.com/{{ dbin_user }}/{{ dbin_repo }}/releases/download",
  ),
  "dbin__vers": tmpl("{{ _release.tag.lstrip('v') }}"),
  "dbin__plat": tmpl("linux-{{ dbin_arch }}"),
} satisfies VarsFile;

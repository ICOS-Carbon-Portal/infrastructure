// Auto-generated by gen-data.ts from ../devops/roles/icos.just/defaults/main.yml
import type { VarsFile } from "../../../../lib/data.ts";
import { expr, tmpl } from "../../../../lib/ansible.ts";

export default {
  "just_upgrade": expr("upgrade_everything | default(False) | bool"),
  "just_version": expr("hostvars.localhost.just_version"),
  "just_url_map": {
    "armv6l": tmpl`https://github.com/casey/just/releases/download/${
      expr("just_version")
    }/just-${expr("just_version")}-arm-unknown-linux-musleabihf.tar.gz`,
    "armv7l": tmpl`https://github.com/casey/just/releases/download/${
      expr("just_version")
    }/just-${expr("just_version")}-armv7-unknown-linux-musleabihf.tar.gz`,
    "aarch64": tmpl`https://github.com/casey/just/releases/download/${
      expr("just_version")
    }/just-${expr("just_version")}-aarch64-unknown-linux-musl.tar.gz`,
    "x86_64": tmpl`https://github.com/casey/just/releases/download/${
      expr("just_version")
    }/just-${expr("just_version")}-x86_64-unknown-linux-musl.tar.gz`,
  },
} satisfies VarsFile;

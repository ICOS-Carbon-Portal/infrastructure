// Auto-generated by gen-data.ts from ../devops/roles/icos.sshlogin/defaults/main.yml
import type { VarsFile } from "../../../../lib/data.ts";
import { expr, rawTmpl, tmpl } from "../../../../lib/ansible.ts";

export default {
  "sshlogin_dst_from": null,
  "sshlogin_dst_command": null,
  "sshlogin_dst_restrict": null,
  "sshlogin_dst_ssh_dir": tmpl`${expr("_dst_user.home")}/.ssh`,
  "sshlogin_dst_known_hosts": tmpl`${expr("sshlogin_dst_ssh_dir")}/known_hosts`,
  "sshlogin_dst_key_file": tmpl`${expr("sshlogin_dst_ssh_dir")}/id_rsa`,
  "sshlogin_dst_key_options": expr(
    "_sshlogin_opt_list | select('ne', '') | join(',')",
  ),
  "_sshlogin_opt_list": [
    tmpl`${rawTmpl("{% if sshlogin_dst_command -%}")} command="${
      rawTmpl("{{ sshlogin_dst_command}}")
    }" ${rawTmpl("{%- endif -%}")}`,
    tmpl`${rawTmpl("{% if sshlogin_dst_from == 'srcip' -%}")} from="${
      expr("sshlogin_src_ip")
    }" ${rawTmpl("{%- elif sshlogin_dst_from -%}")} from="${
      expr("sshlogin_dst_from")
    }" ${rawTmpl("{%- endif -%}")}`,
    tmpl`${rawTmpl("{% if sshlogin_dst_restrict -%}")} restrict ${
      rawTmpl("{%- endif -%}")
    }`,
  ],
  "sshlogin_src_dst": expr("sshlogin_dst"),
  "sshlogin_src_ssh_dir": tmpl`${expr("_src_user.home")}/.ssh`,
  "sshlogin_src_ssh_config": tmpl`${expr("sshlogin_src_ssh_dir")}/config`,
  "sshlogin_src_known_hosts": tmpl`${expr("sshlogin_src_ssh_dir")}/known_hosts`,
  "sshlogin_src_key_file": tmpl`${expr("sshlogin_src_ssh_dir")}/id_rsa`,
  "sshlogin_src_dst_name": expr("sshlogin_dst"),
  "sshlogin_src_dst_host": expr(
    "hostvars[sshlogin_dst].ansible_host | default(sshlogin_dst)",
  ),
  "sshlogin_src_dst_port": expr(
    "hostvars[sshlogin_dst].ansible_port | default(22)",
  ),
} satisfies VarsFile;

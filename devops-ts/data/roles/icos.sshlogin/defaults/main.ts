// Auto-generated by gen-data.ts from ../devops/roles/icos.sshlogin/defaults/main.yml
import type { VarsFile } from "../../../../lib/data.ts";
import { register } from "../../../../lib/ansible.ts";
import { hostvar } from "../../../../lib/ansible.ts";
import { context } from "../../../../lib/context.ts";
import type { ParamVars } from "../../../../lib/paramvars.ts";

interface Self {
  sshlogin_dst_from: unknown;
  sshlogin_dst_command: unknown;
  sshlogin_dst_restrict: unknown;
  sshlogin_dst_ssh_dir: unknown;
  sshlogin_dst_known_hosts: unknown;
  sshlogin_dst_key_file: unknown;
  sshlogin_dst_key_options: unknown;
  _sshlogin_opt_list: unknown;
  sshlogin_src_dst: unknown;
  sshlogin_src_ssh_dir: unknown;
  sshlogin_src_ssh_config: unknown;
  sshlogin_src_known_hosts: unknown;
  sshlogin_src_key_file: unknown;
  sshlogin_src_dst_name: unknown;
  sshlogin_src_dst_host: unknown;
  sshlogin_src_dst_port: unknown;
}
const { V, tmpl, rawTmpl } = context<Self & ParamVars>();

export default {
  "sshlogin_dst_from": null,
  "sshlogin_dst_command": null,
  "sshlogin_dst_restrict": null,
  "sshlogin_dst_ssh_dir": tmpl`${register("_dst_user").home.ref}/.ssh`,
  "sshlogin_dst_known_hosts": tmpl`${V.sshlogin_dst_ssh_dir}/known_hosts`,
  "sshlogin_dst_key_file": tmpl`${V.sshlogin_dst_ssh_dir}/id_rsa`,
  "sshlogin_dst_key_options": rawTmpl(
    "{{ _sshlogin_opt_list | select('ne', '') | join(',') }}",
  ),
  "_sshlogin_opt_list": [
    tmpl`${rawTmpl("{% if sshlogin_dst_command -%}")} command="${
      rawTmpl("{{ sshlogin_dst_command}}")
    }" ${rawTmpl("{%- endif -%}")}`,
    tmpl`${
      rawTmpl("{% if sshlogin_dst_from == 'srcip' -%}")
    } from="${V.sshlogin_src_ip}" ${
      rawTmpl("{%- elif sshlogin_dst_from -%}")
    } from="${V.sshlogin_dst_from}" ${rawTmpl("{%- endif -%}")}`,
    tmpl`${rawTmpl("{% if sshlogin_dst_restrict -%}")} restrict ${
      rawTmpl("{%- endif -%}")
    }`,
  ],
  "sshlogin_src_dst": V.sshlogin_dst,
  "sshlogin_src_ssh_dir": tmpl`${register("_src_user").home.ref}/.ssh`,
  "sshlogin_src_ssh_config": tmpl`${V.sshlogin_src_ssh_dir}/config`,
  "sshlogin_src_known_hosts": tmpl`${V.sshlogin_src_ssh_dir}/known_hosts`,
  "sshlogin_src_key_file": tmpl`${V.sshlogin_src_ssh_dir}/id_rsa`,
  "sshlogin_src_dst_name": V.sshlogin_dst,
  "sshlogin_src_dst_host": hostvar(V.sshlogin_dst).ansible_host.default(
    V.sshlogin_dst,
  ),
  "sshlogin_src_dst_port": hostvar(V.sshlogin_dst).ansible_port.default(22),
} satisfies VarsFile;

// Auto-generated by gen-data.ts from ../devops/roles/icos.nexus/defaults/main.yml
import type { VarsFile } from "../../../../lib/data.ts";
import { context } from "../../../../lib/context.ts";
import type { Globals } from "../../../../lib/globals.ts";
import type { BuiltinVars } from "../../../../lib/builtins.ts";

interface Self {
  nexus_home: string;
  nexus_host_port: string;
  nexus_domains: string;
  nexus_version: string;
  certbot_name: string;
  certbot_domains: string;
  nginxsite_name: string;
  nginxsite_file: string;
}
const { V, expr, tmpl } = context<Self & Globals & BuiltinVars>();

export default {
  "nexus_home": tmpl`${expr("docker_compose_home | default('/docker')")}/nexus`,
  "nexus_host_port": 8881,
  "nexus_domains": [
    "repo.icos-cp.eu",
  ],
  "nexus_version": "2.15.2",
  "certbot_name": "nexus",
  "certbot_domains": V.nexus_domains,
  "nginxsite_name": "nexus",
  "nginxsite_file": "nexus-nginx.conf",
} satisfies VarsFile;

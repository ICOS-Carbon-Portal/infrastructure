// Auto-generated by gen-data.ts from ../devops/roles/icos.golang/defaults/main.yml
import { golang_local_version } from "../../../../lib/paramvars.ts";
import { register } from "../../../../lib/register.ts";
import { jinja, tmpl } from "../../../../lib/template.ts";
import {
  golang_min_version,
  golang_opt_dir,
  golang_version_install,
  type Vars,
} from "../../../../roles/icos.golang/_ctx.ts";

export default {
  "golang_version_install": "1.21.1",
  "golang_min_version": golang_version_install,
  "golang_local_version_ok": jinja`{{ ${golang_local_version} and
   ${golang_local_version} is ${
    register("version").ref
  }(${golang_min_version}, ">=") }}`,
  "golang_apt_version_ok": jinja`{{ golang_apt_version is ${
    register("version").ref
  }(${golang_min_version}, ">=") }}`,
  "golang_url":
    tmpl`https://go.dev/dl/go${golang_version_install}.linux-amd64.tar.gz  `,
  "golang_opt_dir": tmpl`/opt/golang-${golang_version_install}`,
  "golang_bin_dir": tmpl`${golang_opt_dir}/go/bin`,
} satisfies Vars;

// Auto-generated by gen-data.ts from ../devops/roles/icos.conmon/defaults/main.yml
import type { Vars } from "../../../../roles/icos.conmon/_ctx.ts";
import { register } from "../../../../lib/register.ts";
import { jinja } from "../../../../lib/template.ts";
import { context } from "../../../../lib/context.ts";
import type { SharedVars } from "../../../../lib/sharedvars.ts";
import type { ParamVars } from "../../../../lib/paramvars.ts";

interface Self {
  conmon_apt_version_ok: unknown;
  conmon_local_version_ok: unknown;
  conmon_url: unknown;
  conmon_version_install: unknown;
}
const { V, tmpl } = context<Self & SharedVars & ParamVars>();

export default {
  "conmon_version_install": "2.1.4",
  "conmon_local_version_ok": jinja`{{ ${V.conmon_local_version} and
   ${V.conmon_local_version} is ${
    register("version").ref
  }(${V.conmon_min_version}, ">=") }}`,
  "conmon_apt_version_ok": jinja`{{ conmon_apt_version is ${
    register("version").ref
  }(${V.conmon_min_version}, ">=") }}`,
  "conmon_url":
    tmpl`https://github.com/containers/conmon/archive/refs/tags/v${V.conmon_version_install}.tar.gz`,
} satisfies Vars;

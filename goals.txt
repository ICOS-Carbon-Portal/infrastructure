Short term:
  Statically type as much as possible of devops and subest of Ansible in TS
  so that it generates identical yaml, but with statically known variables etc.
  It should be necessary to declare tags/variables somewhere before using them. Even if it's still all
  global scope, we still get an easier overview, and the ability to use an LSP to find all usages.

Long term:
  - Rework the definitions so that they follow a more normal flow, with variables being introduced in scopes etc.

Followup:
  Implement a new orchestrator, like ansible, in typescript, using Ansible tasks and modules for actual execution.
  Instead of jinja templates etc. we will have actual TS.


Possible design:
  - Make playbooks into executable units
  - Playbooks include roles to define logic for plays, and inventories configurations to instantiate those roles.

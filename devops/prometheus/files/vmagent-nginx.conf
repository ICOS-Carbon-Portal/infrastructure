server {
    listen       443 ssl;
    server_name  {{ inventory_hostname }};

    {{ certbot_nginx_conf | indent(4) }}

    location {{ vmagent_pathprefix }} {
        {{ nginxauth_conf | indent(8) }}
        proxy_pass http://127.0.0.1:{{ vm_vm_port }}{{vmagent_pathprefix }};
    }
}

server {
    listen 80;
    server_name  {{ inventory_hostname }};

    return 301 https://$host$request_uri;
}

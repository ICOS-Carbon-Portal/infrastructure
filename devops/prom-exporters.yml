- hosts:
    - fsicos2.lunarc.lu.se
    - fsicos3.lunarc.lu.se
  tasks:
    - name: Setup nginx metrics exporter
      tags: nginx
      import_role:
        name: icos.nginx
        tasks_from: metrics.yml
      when: nginx_metrics_enable

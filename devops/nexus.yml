- hosts: fsicos2.lunarc.lu.se
  vars_files:
    - vault.yml
  tasks:
    - include_role:
        name: icos.nexus
      tags: nexus
    
    - include_role:
        name: icos.bbclient2
      vars:
        bbclient_name: nexus
        bbclient_user: root
        bbclient_home: "{{ nexus_home }}/bbclient"
        bbclient_coldbackup: "{{ nexus_home }}"
      tags: bbclient

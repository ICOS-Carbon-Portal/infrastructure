- name: Install Caddyfiles
  template:
    src: "{{ item }}"
    dest: /etc/caddy/sites/
  loop:
    - Caddyfile.grafana
    - Caddyfile.vm

- name: Reload caddy
  command: caddy reload
  args:
    chdir: /etc/caddy
  changed_when: false

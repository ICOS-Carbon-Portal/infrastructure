# rdfStore derives RDF-log names, graph contexts and replay controls from the same
# cpmeta.instanceServers schema as meta. Reuse the staging amendment so those two views cannot
# drift (including the ICOS Cities collection/document server mappings used by citations).
{% include 'roles/icos.cpmeta/templates/application_staging_amendment.conf' %}

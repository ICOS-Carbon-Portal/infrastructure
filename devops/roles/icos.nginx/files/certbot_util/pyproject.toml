[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "certbot_util"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "click",
]

[project.scripts]
icos-certbot = 'certbot_util:cli'

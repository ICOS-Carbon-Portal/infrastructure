- name: Add ppa:deadsnakes repository
  apt_repository:
    repo: ppa:deadsnakes/ppa

- name: Install python {{ python3_version }}
  apt:
    update_cache: yes
    name:
      - "python{{ python3_version }}-venv"

# # Make sure that pip is available, of the correct version, and not broken.
# - name: Upgrade pip using pip
#   command: |
#     {{ python3_command }} -m ensurepip -U

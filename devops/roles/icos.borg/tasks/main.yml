- name: Download borg binary
  get_url:
    url: "{{ borg_url }}"
    dest: "{{ borg_bin }}"
    mode: +x

- name: Test downloaded binary
  command: |
    {{ borg_bin }} --version
  register: _r
  failed_when: not _r.stdout.startswith('borg')
  changed_when: no

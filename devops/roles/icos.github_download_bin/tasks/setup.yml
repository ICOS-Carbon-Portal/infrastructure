# Required by the github_releases module.
- name: pip install github3
  pip:
    name: github3.py
    state: present

- name: Create downloads directory
  file:
    path: "{{ dbin_download_dir }}"
    state: directory

- name: Remember that we've done setup
  set_fact:
    cacheable: yes
    _download_bin_setup: True

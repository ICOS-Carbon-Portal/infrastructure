cpdata {
	upload{
		folder = "{{ cpdata_home }}/uploads"
		readonlyFolder = "{{ cpdata_filestorage_target }}"
	}
	restheart{
		userDownloadsLogLength = 4
	}

  # Second metadata backend for the portal's dual-view comparison pane
  secondaryMetaHosts {
    ICOS = "meta.fs4vm.icos-cp.eu"
  }
}

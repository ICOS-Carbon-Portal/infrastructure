cpdata {
    upload{
        b2safe.dryRun = true
    }
    restheart.base.ipsToIgnore = []
    postgis.ipsToIgnore = []
}

metacore.envriConfigs.ICOSCities{
	metaHost = "{{ cpmeta_cities_domain }}"
	dataHost = "{{ cpdata_cities_domain }}"
}

authPub.ICOSCities.authHost = "{{ cpauth_cities_domain }}"

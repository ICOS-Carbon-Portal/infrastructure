# Generate certificates using certbot.
- import_tasks: generate.yml
  when: not certbot_fetch_host

# Fetch existing certificates from an existing host. Useful for testing.
- import_tasks: fetch.yml
  when: certbot_fetch_host

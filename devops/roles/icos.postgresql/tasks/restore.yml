- name: Extract "{{ postgresql_container_name }}" backup
  ansible.builtin.script:
    cmd: ../templates/restore.py --host={{ postgresql_backup_host }} --location={{ postgresql_backup_location }} --db={{ postgresql_container_name }}
    executable: /usr/bin/python3

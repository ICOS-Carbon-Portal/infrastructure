akka.remote.artery.canonical{
	port = {{ stiltcluster_port }}
	hostname = "{{ inventory_hostname }}"
}
stiltcluster{
	receptionistAddress = "akka://StiltBoss@{{ inventory_hostname }}:{{stiltcluster_front_port}}/user/receptionist"
	maxCores = {{ stiltcluster_maxcores | default(0) }}
}
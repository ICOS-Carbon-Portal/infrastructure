lxd_vm_default_profiles:
  - default

lxd_vm_default_devices:
  root:
    path: /
    type: disk
    pool: "{{ lxd_vm_name }}"
    size: "{{ lxd_vm_root_size }}"

lxd_vm_default_config:
  limits.cpu: "2"
  limits.memory: "2GB"

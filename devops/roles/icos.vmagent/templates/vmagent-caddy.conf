{{ inventory_hostname }} {
  basicauth bcrypt vmagent {
    {{ vault_vmagent_auth.username }} {{
    vault_vmagent_auth.password | password_hash('bcrypt', vault_bcrypt_salt) }}
  }

  reverse_proxy /vmagent/ {{ vmagent_listen }}
}

- name: Check whether vmagent is installed
  stat:
    path: "{{ vmagent_bin }}/vmagent-prod"
  register: _vmagent

- name: Really install vmagent
  import_tasks: really_install.yml
  when: not _vmagent.stat.exists or (vmagent_upgrade | default(False) | bool)

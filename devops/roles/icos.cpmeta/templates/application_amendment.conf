//should be possible to simply concatenate this file with application.conf (after decoding the latter)

cpmeta{
	stationLabelingService.mailing.mailSendingActive = false
	dataUploadService{
		epicPid.dryRun = true
		handle.dryRun = true
	}
	citations.eagerWarmUp = false

	core.envriConfigs{
		ICOS{
			dataHost = "datatest.icos-cp.eu"
			metaHost = "metatest.icos-cp.eu"
		}
		SITES{
			dataHost = "datatest.fieldsites.se"
			metaHost = "metatest.fieldsites.se"
		}
	}
}
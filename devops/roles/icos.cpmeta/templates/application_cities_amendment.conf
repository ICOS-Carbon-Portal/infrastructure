cpdata {
    upload{
        b2safe.dryRun = true
    }

    downloads{
        skipInit = true // if desirable to run without postgis
    }

    restheart{
        skipInit = false // set to true if desirable to run without restheart
        userDownloadsLogLength = 4
        activityLogUriBases{
            ICOS: "https://cpauth.icos-cp.eu/logs/"
        }
    }
}

cpmeta{
    stationLabelingService.mailing.mailSendingActive = false
    dataUploadService{
        epicPid.dryRun = true
        handle.dryRun = true
    }
    citations.eagerWarmUp = false
}

metacore.envriConfigs{
	ICOSCities{
		dataHost = "localhost"
		metaHost = "localhost"
	}
}

cpauthAuthPub.authHost = "cpauth.icos-cp.eu"

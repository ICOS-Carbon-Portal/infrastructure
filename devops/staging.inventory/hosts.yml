all:
  hosts:
    core_host:
      ansible_host: fsicos2.lunarc.lu.se
      ansible_port: 60565
      lxd_guest_root_keys: "{{ vault_core_host_keys }}"

  children:
    core_server:
      hosts:
        fsicos2.lunarc.lu.se:
      vars:
        important_variable: "staging!"
